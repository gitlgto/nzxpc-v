const getters = {
  // getStorage:function(state){
  //     if(!state.finish){
  //         state.finish=JSON.parse(sessionStorage.getItem(key))
  //     }
  //     return state.finish
  // }
}
export default getters
